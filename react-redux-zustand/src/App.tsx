import { Player } from "./Pages/Player";

import "./styles/global.css";

export function App() {
	/* return {
		<ReduxProvider store={store}>
			<Player />
		</ReduxProvider>
	}; */

	return <Player />;
}
